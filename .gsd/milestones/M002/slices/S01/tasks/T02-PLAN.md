---
estimated_steps: 1
estimated_files: 1
skills_used: []
---

# T02: Persist Profile in Preferences

Update `lib/services/preferences_service.dart` with methods to save and load the `UserProfile`. If no profile exists, return a default one. Create/update tests if applicable.

## Inputs

- `lib/services/preferences_service.dart`

## Expected Output

- `lib/services/preferences_service.dart`

## Verification

`PreferencesService` correctly handles `UserProfile` serialization.

## Observability Impact

None
